package com.example.weatherrecommender.ui.util

/**
 * Coarse weather buckets used for UI accents (e.g. pastel day-chip backgrounds).
 * Aligns with the same WMO ranges as [weatherCodeIcon] / [weatherUiCategory].
 */
enum class WeatherUiCategory {
    CLEAR,
    CLOUDY,
    RAIN,
    SNOW,
    THUNDERSTORM
}
