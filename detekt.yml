build:
  maxIssues: 0
  excludeCorrectable: false

config:
  validation: true

processors:
  active: true

console-reports:
  active: true

style:
  MagicNumber:
    active: false
  MaxLineLength:
    active: false
  WildcardImport:
    active: false

complexity:
  LongMethod:
    threshold: 30
    ignoreAnnotated:
      - Composable
      - Test
  CognitiveComplexMethod:
    threshold: 15
    ignoreAnnotated:
      - Composable
      - Test
  NestedBlockDepth:
    threshold: 4
    ignoreAnnotated:
      - Composable
      - Test
  LongParameterList:
    active: false

exceptions:
  TooGenericExceptionCaught:
    active: false

performance:
  SpreadOperator:
    active: false

naming:
  FunctionNaming:
    # Allow Compose PascalCase (@Composable) and camelCase; ignoreAnnotated needs type resolution.
    functionPattern: '([a-z][a-zA-Z0-9]*)|([A-Z][a-zA-Z0-9]*)'
    ignoreAnnotated:
      - Composable
      - Test
