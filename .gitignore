*.iml
.DS_Store

# Gradle
.gradle
# Throwaway GRADLE_USER_HOME dirs used to isolate agent/CI runs (multi-GB caches).
.gradle-*/

# Build outputs
/build
/build-*/
/captures
.externalNativeBuild
.cxx
.kotlin/

# Local config (machine-specific, may contain SDK paths or keys)
# See local.properties.example for the expected shape.
local.properties
/.idea/
ksp.xml

# Scratch logs and captured tool output
*.log
/.ci-logs*/
compile-out*.txt
test-out.txt
sdk_list.txt

# Local-only generated reference docs (kept out of the repo; README is the source of truth).
/docs/

# Interview prep (local-only)
cheatsheet.md
